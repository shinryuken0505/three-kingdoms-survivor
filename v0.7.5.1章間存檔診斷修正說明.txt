Demo6 v0.7.5.1・章間存檔診斷修正版

修正內容：
1. 修正 headless 自測模式下 save_run_checkpoint() 被提前阻擋，造成「章間存檔建立失效」的假失敗。
2. save_run_checkpoint() 改為回傳成功／失敗，並驗證玩家資料不是空值。
3. save_game_meta() 改為回傳寫入結果，正式遊戲寫檔後主動 flush。
4. 自測模式不碰玩家正式存檔，只在記憶體中建立章間存檔。
5. 新增章間存檔 JSON 序列化與反序列化往返測試。

這次錯誤屬於診斷程式的假陰性；正式遊戲存檔邏輯原本不受 self_test_mode 阻擋。
